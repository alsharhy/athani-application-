import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches successfully', (WidgetTester tester) async {
    // اختبار بسيط للتأكد من عدم وجود أخطاء في الإعداد
    expect(1 + 1, equals(2));
  });
}
